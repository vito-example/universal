<input id="{{ $name }}" type="text" class="form-control" name="{{ $name }}" value="{{ $value }}">
<div class="btn-group" role="group" aria-label="..." style="margin-top: 3px;">
    <button type="button" data-inputid="{{ $name }}" class="btn btn-default popup_selector">
        <i class="fa fa-cloud-upload"></i> Upload</button>
    <button type="button" data-inputid="{{ $name }}" class="btn btn-default clear_elfinder_picker">
        <i class="fa fa-eraser"></i> Clear</button>
</div>
